function sum(a, b) { 

    var sum = a+b ;
    return[sum, ':D' ] }
    console.log (sum(4,7));

    function multiply(a, b){
        var multiply = a*b;
        return[multiply,':D']
    }
    console.log (multiply(5,5));
    function sumAndMultiply(a, b, c){
        var testArray = [2, 3, 4]; 
        return[sumAndMultiply,':D']
    }
    console.log(sumAndMultiply (2+2+5));
    function multiplyArray(a, b, c,) {
        var multiplyArray = [5,5,5];
        return[multiplyArray , ':D']
    }
    console.log(multiplyArray(2*2*6));
    